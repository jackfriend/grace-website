.carousel-container
    .carousel
        ul.carousel__list
            li(v-for="item in items", :style="{ 'background-image': 'url(' + item.image + ')' }").carousel__house
                h1 {{ item.name }}
                
